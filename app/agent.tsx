import {useState} from "react";import {Alert,Pressable,ScrollView,StyleSheet,Text,TextInput,View} from "react-native";import {Screen} from "@/components/Screen";import {colors,typography} from "@/theme/tokens";import {buildAgentPlan} from "@/services/agentPlanner";import {actionLabels} from "@/services/agentTypes";import {executeAgentAction} from "@/services/agentExecutor";import {logAgentAction} from "@/services/agentAudit";export default function Agent(){const[request,setRequest]=useState("");const[plan,setPlan]=useState<any>();const[busy,setBusy]=useState(false);async functionplan(){if(!request.trim())return;setBusy(true);try{setPlan(await buildAgentPlan(request.trim()))}catch(e:any){Alert.alert("Rotean couldn't plan that",e.message)}finally{setBusy(false)}}async functionrun(a:any){try{const ok=await executeAgentAction(a);if(ok)Alert.alert("Done","Rotean completed the action.");else Alert.alert("Cancelled","Nothing was changed.")}catch(e:any){Alert.alert("Not connected yet",e.message)}}return <Screen><ScrollView keyboardShouldPersistTaps="handled"><Text style={s.e}>JARVIS MODE</Text><Text style={s.t}>What should I handle?</Text><Text style={s.sub}>Tell Rotean what you need in normal language. It will prepare actions before changing anything.</Text><TextInput value={request} onChangeText={setRequest} placeholder="Handle my Saturday…" placeholderTextColor={colors.inkMuted} multiline style={s.input}/><Pressable disabled={busy} onPress={plan} style={s.button}><Text style={s.bt}>{busy?"Thinking…":"Make a plan"}</Text></Pressable>{plan&&<View style={s.result}><Text style={s.summary}>{plan.summary}</Text>{plan.actions.map((a:any)=><View key={a.id} style={s.action}><Text style={s.badge}>{actionLabels[a.kind]||"Action"}</Text><Text style={s.title}>{a.title}</Text><Text style={s.body}>{a.description}</Text><Pressable onPress={()=>run(a)} style={s.confirm}><Text style={s.ct}>Review & confirm</Text></Pressable></View>)}</View>}</ScrollView></Screen>}const s=StyleSheet.create({e:{...typography.label,color:colors.green},t:{...typography.h1,color:colors.ink,marginTop:8},sub:{...typography.body,color:colors.inkMuted,marginTop:8},input:{minHeight:120,marginTop:20,padding:16,borderRadius:18,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.line,color:colors.ink,textAlignVertical:"top",fontSize:16},button:{marginTop:12,height:52,borderRadius:14,backgroundColor:colors.green,alignItems:"center",justifyContent:"center"},bt:{...typography.h3,color:colors.white},result:{marginTop:20},summary:{...typography.h2,color:colors.ink},action:{marginTop:12,padding:18,borderRadius:18,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.line},title:{...typography.h3,color:colors.ink},body:{...typography.bodySmall,color:colors.inkMuted,marginTop:5},confirm:{marginTop:14,alignSelf:"flex-start",paddingHorizontal:14,paddingVertical:10,borderRadius:11,backgroundColor:colors.lime},ct:{...typography.label,color:colors.greenDeep}});