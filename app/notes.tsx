import {useEffect,useState} from "react";import {Alert,Pressable,StyleSheet,Text,TextInput,View} from "react-native";import {Screen} from "@/components/Screen";import {colors,typography} from "@/theme/tokens";import {listNotes,createNote} from "@/services/notes";
export default function Notes(){const[notes,setNotes]=useState<any[]>([]);const[content,setContent]=useState("");async function load(){try{setNotes(await listNotes())}catch{Alert.alert("Couldn't load notes","Try again.")}}useEffect(()=>{load()},[]);async function add(){if(!content.trim())return;try{await createNote(content.trim(),"note");setContent("");load()}catch{Alert.alert("Couldn't save","Try again.")}}return <Screen><Text style={s.e}>MEMORY</Text><Text style={s.t}>Notes</Text><Text style={s.sub}>Capture things before they disappear.</Text><TextInput multiline value={content} onChangeText={setContent} placeholder="Write something…" style={s.editor}/><Pressable onPress={add} style={s.b}><Text style={s.bt}>Save note</Text></Pressable>{notes.map(n=><View key={n.id} style={s.card}><Text style={s.n}>{n.content}</Text></View>)}</Screen>}const s=StyleSheet.create({e:{...typography.label,color:colors.green},t:{...typography.h1,color:colors.ink,marginTop:8},sub:{...typography.body,color:colors.inkMuted,marginTop:8},editor:{marginTop:28,minHeight:120,borderWidth:1,borderColor:colors.line,borderRadius:18,backgroundColor:colors.surface,padding:16,textAlignVertical:"top",color:colors.ink,fontSize:16},b:{height:50,borderRadius:14,backgroundColor:colors.green,alignItems:"center",justifyContent:"center",marginTop:12},bt:{...typography.h3,color:colors.white},card:{padding:18,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.line,borderRadius:18,marginTop:12},n:{...typography.body,color:colors.ink}});