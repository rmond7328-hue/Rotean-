import {useEffect,useState} from "react";import {StyleSheet,Text,View} from "react-native";import {Screen} from "@/components/Screen";import {colors,typography} from "@/theme/tokens";import {getPersonalProfile} from "@/services/personalModel";export default function PersonalModel(){const[d,setD]=useState<any>();useEffect(()=>{getPersonalProfile().then(setD).catch(()=>{})},[]);return <Screen><Text style={s.e}>PERSONAL MODEL</Text><Text style={s.t}>Rotean is learning.</Text><Text style={s.sub}>It learns from patterns and information you choose to share. It should never pretend to know something it doesn't.</Text><View style={s.card}><Text style={s.n}>{d?.memories?.length||0}</Text><Text style={s.label}>useful memories</Text></View><View style={s.card}><Text style={s.n}>{d?.behavior?.length||0}</Text><Text style={s.label}>recent signals</Text></View><Text style={s.note}>As Rotean sees more of your real life, recommendations can become more personal without requiring you to configure everything manually.</Text></Screen>}const s=StyleSheet.create({e:{...typography.label,color:colors.green},t:{...typography.h1,color:colors.ink,marginTop:8},sub:{...typography.body,color:colors.inkMuted,marginTop:8},card:{marginTop:14,padding:20,borderRadius:18,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.line},n:{...typography.h1,color:colors.green},label:{...typography.bodySmall,color:colors.inkMuted,marginTop:2},note:{...typography.bodySmall,color:colors.inkMuted,marginTop:22,lineHeight:20}});