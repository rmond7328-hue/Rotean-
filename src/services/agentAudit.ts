import {supabase} from "@/lib/supabase";import {AgentAction,AgentActionStatus} from "./agentTypes";export async function logAgentAction(action:AgentAction,status:AgentActionStatus){const {data:{user}}=await supabase.auth.getUser();if(!user)return null;const {data,error}=await supabase.from("agent_action_log").insert({user_id:user.id,action_id:action.id,action_type:action.kind,status,payload:action.payload||{}}).select().single();if(error)throw error;return data}