import {confirmAndExecute} from "./actionConfirm";import {AgentAction} from "./agentTypes";import {logAgentAction} from "./agentAudit";export async function executeAgentAction(action:AgentAction){await logAgentAction(action,"proposed");if(action.kind==="task_complete"&&action.payload.taskId){const ok=await confirmAndExecute({kind:"complete_task",taskId:action.payload.taskId,label:action.title});await logAgentAction(action,ok?"completed":"failed");return ok}if(action.kind==="task_reschedule"&&action.payload.taskId&&action.payload.dueAt){const ok=await confirmAndExecute({kind:"postpone_task",taskId:action.payload.taskId,dueAt:action.payload.dueAt,label:action.title});await logAgentAction(action,ok?"completed":"failed");return ok}throw new Error("This action is planned but its integration is not connected yet.")}