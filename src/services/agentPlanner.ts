import {buildContext} from "@/services/context";import {AgentAction,AgentPlan} from "./agentTypes";export async function buildAgentPlan(request:string):Promise<AgentPlan>{const c=await buildContext();const lower=request.toLowerCase();const actions:AgentAction[]=[];const dueTask=(c.tasks as any[]).find(t=>/finish|complete|task|assignment|project/.test(lower)&&t.status!=="completed");if(dueTask)actions.push({id:"task-1",kind:"task_complete",title:`Complete: ${String(dueTask.title||"task")}`,description:"Rotean found a matching active task. Review before completing it.",payload:{taskId:dueTask.id},requiresConfirmation:true});if(lower.includes("remind"))actions.push({id:"reminder-1",kind:"reminder_create",title:"Rotean reminder",description:"Create a reminder for this request. The exact time will be confirmed before creation.",payload:{request},requiresConfirmation:true});if(lower.includes("calendar")||lower.includes("schedule"))actions.push({id:"calendar-1",kind:"calendar_create",title:"Create a calendar event",description:"Prepare the requested calendar commitment.",payload:{request},requiresConfirmation:true});return {summary:actions.length?`I found ${actions.length} possible action${actions.length===1?"":"s"} using your current context.`:"I understand the request, but I don't have enough information to safely change anything yet.",actions,createdAt:new Date().toISOString()}}