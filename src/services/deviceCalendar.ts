import * as Calendar from "expo-calendar";import {syncCalendarEvents} from "./calendarIntegration";export async function connectDeviceCalendar(){const {status}=await Calendar.requestCalendarPermissionsAsync();if(status!=="granted")throw new Error("Calendar permission was not granted.");const calendars=await Calendar.getCalendarsAsync(Calendar.EntityTypes.EVENT);const writable=calendars.filter(c=>c.allowsModifications&&c.entityType===Calendar.EntityTypes.EVENT);const ids=calendars.map(c=>c.id);const start=new Date();start.setHours(0,0,0,0);const end=new Date(start);end.setDate(end.getDate()+30);const events=await Promise.all(ids.map(id=>Calendar.getEventsAsync([id],start,end)));return syncCalendarEvents(events.flat())}