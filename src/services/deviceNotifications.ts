import * as Notifications from "expo-notifications";Notifications.setNotificationHandler({handleNotification:async()=>({shouldShowBanner:true,shouldShowList:true,shouldPlaySound:false,shouldSetBadge:false})});export async function enableNotifications(){const current=await Notifications.getPermissionsAsync();if(current.status==="granted")return current.status;const requested=await Notifications.requestPermissionsAsync();if(requested.status!=="granted")throw new Error("Notification permission was not granted.");return requested.status}export async function scheduleRoteanNotification(title:string,body:string,when:Date,data?:Record<string,string>){if(when.getTime()<=Date.now())return null;await enableNotifications();return Notifications.scheduleNotificationAsync({content:{title,body,data,sound:false},trigger:{type:Notifications.SchedulableTriggerInputTypes.DATE,date:when}})}export async function cancelRoteanNotification(id:string){return Notifications.cancelScheduledNotificationAsync(id)}