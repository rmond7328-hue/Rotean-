import * as Notifications from "expo-notifications";export async function enableNotifications(){const {status}=await Notifications.requestPermissionsAsync();if(status!=="granted")throw new Error("Notification permission was not granted.");return status}export async function scheduleRoteanNotification(title:string,body:string,when:Date,data?:Record<string,string>){if(when<=new Date())return null;return Notifications.scheduleNotificationAsync({content:{title,body,data},trigger:{type:Notifications.SchedulableTriggerInputTypes.DATE,date:when}})}