import {enableNotifications} from "./deviceNotifications";import {connectDeviceCalendar} from "./deviceCalendar";import {getCurrentLocation} from "./deviceLocation";export async function connectRoteanDevices(){const result:{notifications:boolean;calendar:number;location:boolean}={notifications:false,calendar:0,location:false};try{await enableNotifications();result.notifications=true}catch{}try{result.calendar=(await connectDeviceCalendar()).length}catch{}try{await getCurrentLocation();result.location=true}catch{}return result}