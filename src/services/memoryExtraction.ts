import {supabase} from "@/lib/supabase";export async function extractAndSaveMemory(text:string){const {data:{session}}=await supabase.auth.getSession();if(!session)throw new Error("Sign in first.");const r=await fetch((process.env.EXPO_PUBLIC_SUPABASE_URL||"")+"/functions/v1/rotean-memory",{method:"POST",headers:{Authorization:"Bearer "+session.access_token,"Content-Type":"application/json"},body:JSON.stringify({text})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Memory extraction failed");return d;}