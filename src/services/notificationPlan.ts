export type NotificationIntent={title:string;body:string;when:Date;kind:"leave"|"task"|"plan"|"habit"|"info";priority:"low"|"normal"|"high"};export function shouldNotify(i:NotificationIntent,now=new Date()){if(i.when.getTime()<=now.getTime())return false;return i.priority!=="low"||i.kind==="leave"}export function buildNotificationCopy(i:NotificationIntent){return {title:i.title,body:i.body,data:{kind:i.kind}}}