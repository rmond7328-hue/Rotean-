"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { getCalendarEvents, getGoals, getHabits, getTasks } from "@/lib/data/life";

export default function AppHomePage() {
  const [tasks, setTasks] = useState<any[]>([]); const [events, setEvents] = useState<any[]>([]); const [habits, setHabits] = useState<any[]>([]); const [goals, setGoals] = useState<any[]>([]); const [error, setError] = useState("");
  const today = useMemo(() => new Date(), []);
  useEffect(() => { const start = new Date(); start.setHours(0,0,0,0); const end = new Date(start); end.setDate(end.getDate()+1); Promise.all([getTasks(), getGoals(), getHabits(), getCalendarEvents(start.toISOString(), end.toISOString())]).then(([t,g,h,e]) => { const firstError = t.error || g.error || h.error || e.error; if (firstError) setError(firstError.message); setTasks(t.data || []); setGoals(g.data || []); setHabits(h.data || []); setEvents(e.data || []); }).catch((e) => setError(e.message || "Could not load your day.")); }, []);
  const due = tasks.filter(t => t.status !== "completed" && t.due_at && new Date(t.due_at).toDateString() === today.toDateString()).length;
  const open = tasks.filter(t => t.status !== "completed").slice(0, 4);
  return <div className="space-y-8"><section className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[var(--green)]">{today.toLocaleDateString(undefined,{weekday:"long",month:"long",day:"numeric"})}</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.04em] sm:text-4xl">Good {today.getHours()<12?"morning":today.getHours()<18?"afternoon":"evening"}.</h1><p className="mt-2 max-w-xl text-sm leading-6 text-[var(--muted)]">Here’s the shape of your life today. Rotean will turn this data into decisions as the intelligence layer comes online.</p></div><Link href="/app/tasks" className="w-fit rounded-xl bg-[var(--green)] px-4 py-2.5 text-sm font-semibold text-white">+ Add task</Link></section>{error && <p role="alert" className="rounded-xl border border-[var(--line)] bg-[var(--surface)] px-4 py-3 text-sm text-[var(--danger)]">{error}</p>}<section className="grid gap-4 xl:grid-cols-[1.45fr_0.75fr]"><div className="rounded-3xl bg-[var(--green)] p-6 text-white sm:p-8"><span className="inline-flex rounded-full bg-white/10 px-3 py-1 text-[10px] font-semibold uppercase tracking-[0.16em]">What’s next?</span><h2 className="mt-5 max-w-2xl text-2xl font-semibold tracking-[-0.035em] sm:text-3xl">Rotean is learning the shape of your day.</h2><p className="mt-3 max-w-xl text-sm leading-6 text-white/70">Your tasks, commitments, goals and habits are now connected. Phase 5 will use them to understand your patterns and make better recommendations.</p><div className="mt-7 flex flex-wrap gap-2"><Link href="/app/whats-next" className="rounded-xl bg-[var(--lime)] px-4 py-2.5 text-xs font-bold text-[var(--green-deep)]">See what’s next</Link><Link href="/app/jarvis" className="rounded-xl border border-white/15 px-4 py-2.5 text-xs font-semibold">Open JARVIS</Link></div></div><div className="rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-6 sm:p-7"><p className="text-xs font-semibold uppercase tracking-[0.15em] text-[var(--muted)]">Today</p><div className="mt-6 grid grid-cols-2 gap-3">{[[due,"tasks due"],[events.length,"events"],[habits.length,"habits"],[goals.filter(g=>g.status!=="completed").length,"active goals"]].map(([n,l])=><div key={String(l)} className="rounded-2xl bg-[var(--background)] p-4"><p className="text-2xl font-semibold">{n}</p><p className="mt-1 text-xs text-[var(--muted)]">{l}</p></div>)}</div></div></section><section className="grid gap-4 lg:grid-cols-[1.15fr_0.85fr]"><div className="rounded-3xl border border-[var(--line)] bg-[var(--surface)] overflow-hidden"><div className="flex items-center justify-between border-b border-[var(--line)] px-5 py-4"><h2 className="font-semibold">Open tasks</h2><Link href="/app/tasks" className="text-xs font-semibold text-[var(--green)]">View all →</Link></div>{open.length ? open.map(t=><div key={t.id} className="flex items-center gap-3 border-b border-[var(--line)] px-5 py-4 last:border-0"><span className="h-2 w-2 rounded-full bg-[var(--green)]"/><span className="min-w-0 flex-1 truncate text-sm">{t.title}</span>{t.estimated_minutes && <span className="text-xs text-[var(--muted)]">{t.estimated_minutes}m</span>}</div>) : <p className="px-5 py-8 text-sm text-[var(--muted)]">Nothing waiting. That’s a good thing.</p>}</div><div className="rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-5"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-[var(--muted)]">Life data</p><div className="mt-4 grid gap-2 sm:grid-cols-2">{[["Projects","/app/projects"],["Goals","/app/goals"],["Habits","/app/habits"],["Memory","/app/memory"]].map(([label,href])=><Link key={label} href={href} className="rounded-2xl bg-[var(--background)] p-4 text-sm font-semibold hover:bg-[var(--soft-green)]">{label}<span className="float-right text-[var(--muted)]">→</span></Link>)}</div></div></section></div>;
}
