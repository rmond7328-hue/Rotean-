"use client";
import{FormEvent,useEffect,useState}from"react";import{createProject,deleteProject,getProjects,updateProject,type Project}from"@/lib/data/life";
export default function ProjectsPage(){const[items,setItems]=useState<Project[]>([]),[title,setTitle]=useState(""),[description,setDescription]=useState(""),[error,setError]=useState("");const load=async()=>{const{data,error}=await getProjects();if(error)setError(error.message);else setItems((data||[])as Project[])};useEffect(()=>{load()},[]);async function add(e:FormEvent){e.preventDefault();if(!title.trim())return;const{error}=await createProject(title,description);if(error)setError(error.message);else{setTitle("");setDescription("");load()}}return<section><header className="mb-8"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[var(--green)]">Work in motion</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.045em] sm:text-4xl">Projects.</h1><p className="mt-2 text-sm text-[var(--muted)]">Projects connect everyday tasks to bigger outcomes.</p></header><form onSubmit={add} className="mb-7 rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-4"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Project name" className="mb-2 w-full rounded-xl border border-[var(--line)] px-4 py-3"/><input value={description} onChange={e=>setDescription(e.target.value)} placeholder="What is this project for? (optional)" className="mb-3 w-full rounded-xl border border-[var(--line)] px-4 py-3"/><button className="rounded-xl bg-[var(--green)] px-5 py-3 font-semibold text-white">Create project</button></form>{error&&<p className="mb-5 text-sm text-[var(--danger)]">{error}</p>}<div className="grid gap-3 md:grid-cols-2">{items.length===0?<div className="rounded-2xl border border-dashed border-[var(--line)] p-10 text-center text-sm text-[var(--muted)] md:col-span-2">No projects yet.</div>:items.map(p=><article key={p.id} className="rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-5"><div className="flex items-start gap-3"><div className="flex-1"><h2 className="font-semibold">{p.title}</h2>{p.description&&<p className="mt-2 text-sm text-[var(--muted)]">{p.description}</p>}<select value={p.status} onChange={async e=>{await updateProject(p.id,{status:e.target.value});load()}} className="mt-4 rounded-lg border border-[var(--line)] bg-[var(--surface)] px-2 py-1 text-xs"><option value="active">Active</option><option value="paused">Paused</option><option value="completed">Completed</option></select></div><button onClick={async()=>{await deleteProject(p.id);load()}} className="text-xs text-[var(--muted)]">Delete</button></div></article>)}</div></section>}
